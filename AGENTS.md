# このリポジトリで作業するエージェントへの指示

## 実装と依存関係

- 設計変更の依頼がない限り、Azure CLIに対応する公開APIの階層を維持し、メソッドごとのパッケージやService／Repository層は追加しない。
- APIの動作は[README](README.md)、構造と設計理由は[アーキテクチャ](docs/architecture.md)を参照する。
- 依存関係は `go.mod` の固定バージョンを基準にする。依存更新の依頼がない作業で `latest` に更新しない。
- 依存関係を変更した場合は `mise run tidy` を実行し、更新された `go.mod` と `go.sum` をコミット対象に含める。
- SDKのメジャーバージョンを変更する場合は、公開しているSDKモデルの型エイリアスへの影響を確認する。
- Goのmodule pathを変更する場合は、`go.mod` のmodule行とGoソースの対応するimportをまとめて変更する。

## 検証

- 開発タスクには `mise.toml` を使用する。タスク一覧と環境準備は[READMEの開発タスク](README.md#開発タスク)を参照する。
- Goソースを変更した場合は `mise run fmt` と `mise run check` を実行する。
- 検証結果は実行した範囲と環境を明記する。`mise run check` の成功を、race detectorや実Azureへの接続の検証として扱わない。

## ドキュメント

- エージェントへの継続的な作業指示はこのファイルに記載する。
- READMEには利用手順とAPI仕様、`docs/` には設計理由・出典・検証記録を記載する。
- 完了済みの初回作成・公開作業の指示は残さない。過去の検証結果は日付を付けた記録として保持する。
